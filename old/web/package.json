{
  "name": "scottapow",
  "version": "1.0.0",
  "description": "Personal playground for all types of web projects",
  "main": "main.go",
  "type": "module",
  "devDependencies": {
    "@pandacss/dev": "^1.2.0",
    "tailwindcss": "^4.0.12",
    "typescript": "^5.5.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css": "npx @tailwindcss/cli -i ./input.css -o ./public/output.css",
    "build:css:watch": "npx @tailwindcss/cli -i ./input.css -o ./public/output.css --watch",
    "build:js": "tsc && esbuild public/main.ts --bundle --outdir=static/js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/scottapow/scottapow.git"
  },
  "author": "Scott Powell",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/scottapow/scottapow/issues"
  },
  "homepage": "https://github.com/scottapow/scottapow#readme",
  "dependencies": {
    "@tailwindcss/cli": "^4.0.12",
    "@types/node": "^20.14.9",
    "esbuild": "^0.25.0"
  }
}